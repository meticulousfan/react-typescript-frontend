import React from 'react'

export const RecordingNotSupported = () => (
    <p>
        You are using an old browser that doesn't support recording. We suggest using latest Google Chrome
        browser.
    </p>
)
